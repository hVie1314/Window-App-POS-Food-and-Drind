<Page
    x:Class="POS.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="Beige">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="/Assets/image/logo.jpg" Stretch="Uniform" MaxWidth="250" MaxHeight="250"/>
            </StackPanel>
        </Grid>
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBox x:Name="UsernameTextBox" Header="Tài khoản"
                 HorizontalAlignment="Center" VerticalAlignment="Center"
                 Margin="10"
                 Width="200"
                 BorderThickness="0 0 0 1"
                CornerRadius="0"/>

        <PasswordBox x:Name="PasswordBox" Header="Mật khẩu"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Margin="10" 
                     Width="200"
                     BorderThickness="0 0 0 1"
                     CornerRadius="0"/>

        <Button Content="Đăng nhập" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Margin="10" Click="OnLoginClick"
                Padding="12"/>
        </StackPanel>
    </Grid>
</Page>
